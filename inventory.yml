all:
  vars:
    # Instruct Ansible to connect directly to the containers rather than using SSH
    ansible_connection: docker
    ansible_python_interpreter: /usr/bin/python3
  children:
    cluster:
      hosts:
        app-vm:
        db-vm:
        lb-vm: